import 'package:flutter/material.dart';

class LightColors {
  const LightColors();

  ///Button color Theme.of(context).colorScheme.tertiary
  static const lilacTitan = Color(0xFFE1E6FF);

  ///Button color Theme.of(context).colorScheme.tertiaryContainer
  static const blueIndigo = Color(0xFF5165C7);

  ///Button color Theme.of(context).colorScheme.onTertiaryContainer
  static const greyAlto = Color(0xFFCECECE);

  ///Text color Theme.of(context).primaryColor
  static const greyDark = Color(0xFF333333);

  ///Hint color Theme.of(context).hintColor
  static const mainBlack = Color(0xFF939393);

  ///Divider color Theme.of(context).dividerColor
  static const beigeDustStorm = Color(0xFFE7D1CA);

  ///Background Theme.of(context).scaFFoldBackgroundColor
  static const beigeBizarre = Color(0xFFF2E4DF);

  ///Button background Theme.of(context).colorScheme.secondary,
  static const orangeOutrageous = Color(0xFFFF5B35);

  ///Error color Theme.of(context).colorScheme.error,
  static const redCrimson = Color(0xFFEB1447);

  static const backgroundBlue = Color(0xffE1E6FF);

  ///Delete color Theme.of(context).colorScheme.onInverseSurface,
  static const redOrange = Color(0xFFFF3B30);
}
